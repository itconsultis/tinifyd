#!/usr/bin/env bash

SCRIPT_DIR=$(dirname $(readlink -f "${BASH_SOURCE[0]}"))

node --harmony "$SCRIPT_DIR/../node_modules/db-migrate/bin/db-migrate" \
    --env=default \
    --config=./database/config.json \
    --migrations-dir=./database/migrations \
    $*

